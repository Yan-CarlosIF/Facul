#include <stdio.h>
#include <math.h>

int main(){
    int num,m;
    printf("Digite um numero:");
    scanf("%d",&num);
    printf("Digite um numero que deseja elevar o primeiro:");
    scanf("%d",&m);
    printf("\n%lf REPETICAO EH MEU OVO",pow(num,m));
    return 0;
}