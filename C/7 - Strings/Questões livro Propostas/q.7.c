/*
Faça um programa que receba uma frase e mostre quantas letras diferentes ela contém.
*/

#include <stdio.h>
#include <string.h>
#include <ctype.h>

int letrasDif(char *frase)
{
    char aux[100];
    int cont = 0, tam = strlen(frase);

    for (int i = 0; i < tam; i++) 
    {
        // if ()
    }
}

int main()
{
    char frase[100];

    printf("Digite uma frase:");
    gets(frase);
    return 0;
}